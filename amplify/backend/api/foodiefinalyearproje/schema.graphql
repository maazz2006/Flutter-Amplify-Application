input AMPLIFY {
  globalAuthRule: AuthRule = {allow: public}
}

type Suggester @model @auth(rules: [{allow: public}]) {
  id: ID!
  resturantName: String!
  specials: String!
  address: String!
  lat: String!
  long: String!
  imageUrl: String!
} 